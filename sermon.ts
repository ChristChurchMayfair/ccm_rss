export type Sermon = {
    id: string,
    title: string,
    passage?: string,
    seriesTitle: string,
    seriesSubtitle?: string,
    preachedAt: string,
    duration: number,
    link: string,
    imageUrl?: string,
    author: string,
    event: string,
}